<% layout("/layout/boilerplate") %>
<body>
    <div class="row">
        <div class="col-8 offset-2">
            <form method="POST" action="/listing/<%=listing._id%>?_method=PUT">
                <div class="mb-2">
                    <label for="title" class="form-label">Title</label>
                    <input name="listing[title]" value="<%=listing.title%>" placeholder="enter the title" class="form-control">
                </div>
        
                <div class="mb-2"> 
                    <label for="description" class="form-label">Description</label>
                    <textarea name="listing[description]" placeholder="enter the description" class="form-control"> <%=listing.title%> </textarea>
                </div>
        
                <div class="mb-2">
                    <label for="image" class="form-label">Image Link</label>
                    <input name="listing[image]" placeholder="put the image link" value="<%=listing.image%>" class="form-control" >
                </div>

                <div class="row">
                    <div class="mb-2 col-md-4">
                        <label for="price" class="form-label">Price</label>
                        <input name="listing[price]" placeholder="add price" value="<%=listing.price%>" class="form-control">
                    </div>

                    <div class="mb-2 col-md-8">
                        <label for="country" class="form-label">Country</label>
                        <input name="listing[country]" placeholder="add country name" value="<%=listing.country%>" class="form-control">
                    </div>
                </div>
                
                <div class="mb-2">
                    <label for="location" class="form-label">Location</label>
                    <input name="listing[location]" placeholder="add location" value="<%=listing.location%>" class="form-control" >
                </div>
        
            <button class="btn btn-dark edit-btn mt-3">submit</button>
        </form>
        </div>

    </div>
    
</body>
</html>